apiVersion: v1
kind: Pod
metadata:
  name: pod-volume
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      volumeMounts:
        - mountPath: /nginx-volume
          name: primeiro-volume
    - name: jenkins-container
      image: jenkins:alpine
      volumeMounts:
        - mountPath: /jenkins-volume
          name: primeiro-volume
  volumes:
    - name: primeiro-volume
      hostPath:
        path: /C/WAVES/portfolio/finish2024/kubernetes/course-2/volume
        type: Directory
